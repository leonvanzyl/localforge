export default function HomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <div className="max-w-xl text-center">
        <h1 className="text-4xl font-semibold tracking-tight">
          LocalForge
        </h1>
        <p className="mt-4 text-muted-foreground">
          Autonomous coding harness powered by local LLMs. The app shell,
          project management, kanban, AI bootstrapper, orchestrator, and
          Playwright integration will be built feature-by-feature by the
          coding agents.
        </p>
      </div>
    </main>
  );
}
